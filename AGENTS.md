## Project
- Description: This project aims to develop an AI text-based application using TypeScript, React, and Node.js.

## Verification commands (unchanged)
- Dev: `npm run compile`
- Build: `vsce package && code --install-extension ollama-ai-assistant-1.0.0.vsix`
- Test: `npm test`
- Lint: `npm run lint`

## Output style
- Be direct.
- Surface assumptions early.
- Call out risks, migrations, and breaking changes explicitly.
- When a task is ambiguous, prefer the smallest safe implementation.

## Roles enabled (unchanged)
- plan
- review
- test
- code
